# Top-level dependencies for Script/05_tf_inference.py.
# Pinned to the versions verified against this pipeline; unlike the R
# scripts' install-if-missing helpers, Python projects conventionally pin
# versions explicitly and install into an isolated virtual environment
# rather than auto-installing into a shared/global package library.
#
# Setup:
#   cd Script
#   python3 -m venv ../.venv
#   source ../.venv/bin/activate
#   pip install -r requirements.txt
decoupler==2.1.6
pandas==3.0.3
matplotlib==3.11.0
